
syntax = "proto3";
package cosmos;
import "tendermint/blockchain/types.proto";

// The greeting service definition.
service Greeter {
  // Sends a greeting
  rpc SayHello (tendermint.blockchain.BlockRequest) returns (tendermint.blockchain.BlockResponse) {}
}
  